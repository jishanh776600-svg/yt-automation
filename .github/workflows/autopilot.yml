name: YouTube Shorts Cloud Publisher
# ==============================================================================
# SCHEDULED CLOUD PUBLISHER: Autonomous Releaser (4x Daily Slots)
# Strictly publishes 4 public Shorts daily directly from Google Drive Vault.
# Execution Time: ~30-45 seconds (Ultra-fast: Claim -> Download -> Upload -> Move)
# ==============================================================================

on:
  schedule:
    # 3 scheduled publishing slots daily:
    # 06:00 UTC (11:30 AM IST) | 11:00 UTC (4:30 PM IST) | 15:00 UTC (8:30 PM IST)
    - cron: '0 6,11,15 * * *'
  workflow_dispatch: # Allows manual on-demand publishing from GitHub Web / Mobile

permissions:
  contents: write

concurrency:
  group: pipeline-cloud-execution
  cancel-in-progress: false

jobs:
  publish_scheduled_short:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Restore OAuth Credentials & Environment Secrets
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_SECONDARY: ${{ secrets.GEMINI_API_KEY_SECONDARY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          TOKEN_JSON: ${{ secrets.TOKEN_JSON }}
          CLIENT_SECRET_JSON: ${{ secrets.CLIENT_SECRET_JSON }}
        run: |
          echo "PEXELS_API_KEY=${PEXELS_API_KEY}" > .env
          echo "GEMINI_API_KEY=${GEMINI_API_KEY}" >> .env
          if [ -n "${GEMINI_API_KEY_SECONDARY}" ]; then
            echo "GEMINI_API_KEY_SECONDARY=${GEMINI_API_KEY_SECONDARY}" >> .env
          fi
          if [ -n "${DEEPSEEK_API_KEY}" ]; then
            echo "DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}" >> .env
          fi
          echo "TEST_MODE=false" >> .env
          echo "${TOKEN_JSON}" > token.json
          echo "${CLIENT_SECRET_JSON}" > client_secret.json

      - name: Download & Verify Canonical Database from Cloud Vault
        run: |
          python -m core.database_sync download

      - name: Claim and Schedule Ready Shorts from Google Drive Vault
        run: |
          python main.py --schedule-ready

      - name: Upload & Synchronize Database to Cloud Vault
        if: always()
        run: |
          python -m core.database_sync upload

      - name: Commit Analytics Feedback & Learning Logs
        if: always()
        run: |
          git config --global user.name 'YouTube Publisher Bot'
          git config --global user.email 'bot@github-actions.local'
          git add data/LEARNING_LOG.md 2>/dev/null || true
          git commit -m "Auto-update publisher learning log and state [skip ci]" || echo "No changes to commit"
          git push origin main || echo "Push skipped"
